<UserControl x:Class="IndustrialDAQ.UI.Views.VisionInspectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:IndustrialDAQ.UI.Views"
             Background="{DynamicResource MainBgBrush}">
    <UserControl.Resources><BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <Style x:Key="VisionButton" TargetType="Button"><Setter Property="Padding" Value="14,7"/><Setter Property="Margin" Value="0,0,8,0"/><Setter Property="Background" Value="{DynamicResource BrushBgSecondary}"/><Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/><Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/><Setter Property="Cursor" Value="Hand"/></Style>
        <Style x:Key="MetricCard" TargetType="Border"><Setter Property="Background" Value="{DynamicResource CardBgBrush}"/><Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/><Setter Property="BorderThickness" Value="1"/><Setter Property="CornerRadius" Value="7"/><Setter Property="Padding" Value="16"/><Setter Property="Margin" Value="0,0,12,0"/></Style>
        <Style x:Key="SectionBorder" TargetType="Border"><Setter Property="Background" Value="{DynamicResource CardBgBrush}"/><Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/><Setter Property="BorderThickness" Value="1"/><Setter Property="CornerRadius" Value="7"/></Style>
    </UserControl.Resources>
    <Grid>
    <Grid Margin="18" Visibility="{Binding IsWorkbenchVisible, Converter={StaticResource BoolToVisibility}}"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="3*"/><RowDefinition Height="2*"/></Grid.RowDefinitions>
        <!-- 标题与运行状态 -->
        <Grid Grid.Row="0" Margin="0,0,0,12"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal"><TextBlock Text="机器视觉检测" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/><Border Width="1" Height="22" Background="{DynamicResource BorderBrush}" Margin="14,0"/><TextBlock Text="OpenCV 顺序配方" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/></StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal"><Ellipse Width="9" Height="9" Fill="{Binding CameraStatusColor}"/><TextBlock Text="{Binding CameraStatusText, StringFormat={} {0}    }" Foreground="{DynamicResource TextSecondaryBrush}"/><Ellipse Width="9" Height="9" Fill="{DynamicResource InfoBrush}"/><TextBlock Text=" 视觉运行时在线" Foreground="{DynamicResource TextSecondaryBrush}"/></StackPanel>
        </Grid>
        <!-- 操作栏 -->
        <Border Grid.Row="1" Background="{DynamicResource BrushBgTertiary}" Padding="12" CornerRadius="6" Margin="0,0,0,12"><Grid><StackPanel Orientation="Horizontal"><Button Style="{StaticResource VisionButton}" Content="▶ 开始检测" Command="{Binding StartCommand}" Background="{DynamicResource InfoBrush}" Foreground="White" Visibility="{Binding CanModify, Converter={StaticResource BoolToVisibility}}"/><Button Style="{StaticResource VisionButton}" Content="{Binding PauseButtonText}" Command="{Binding PauseCommand}" Visibility="{Binding CanModify, Converter={StaticResource BoolToVisibility}}"/><Button Style="{StaticResource VisionButton}" Content="↻ 重置统计" Command="{Binding ResetCommand}" Visibility="{Binding CanModify, Converter={StaticResource BoolToVisibility}}"/><Button Style="{StaticResource VisionButton}" Content="◷ 手动触发一次" Command="{Binding TriggerCommand}" Visibility="{Binding CanModify, Converter={StaticResource BoolToVisibility}}"/><Button Style="{StaticResource VisionButton}" Content="🔐 登录后新增视觉任务" Command="{Binding LoginToConfigureCommand}" Background="#B45309" Foreground="White" Visibility="{Binding CannotModify, Converter={StaticResource BoolToVisibility}}"/></StackPanel><StackPanel Orientation="Horizontal" HorizontalAlignment="Right"><Button Style="{StaticResource VisionButton}" Content="⚙ 相机配置" Command="{Binding ConfigureCameraCommand}" Visibility="{Binding CanModify, Converter={StaticResource BoolToVisibility}}"/><Button Style="{StaticResource VisionButton}" Content="＋ 新增视觉任务" Command="{Binding AddTaskCommand}" Background="{DynamicResource InfoBrush}" Foreground="White" Visibility="{Binding CanModify, Converter={StaticResource BoolToVisibility}}"/><Button Style="{StaticResource VisionButton}" Content="⚙ 配置任务" Command="{Binding ConfigureCommand}" Visibility="{Binding CanModify, Converter={StaticResource BoolToVisibility}}"/><Button Style="{StaticResource VisionButton}" Content="刷新" Command="{Binding RefreshCommand}"/></StackPanel></Grid></Border>
        <!-- 指标卡 -->
        <Grid Grid.Row="2" Margin="0,0,0,12"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource MetricCard}"><StackPanel Orientation="Horizontal"><TextBlock Text="▥" FontSize="28" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,18,0"/><StackPanel><TextBlock Text="{Binding TotalCount}" FontSize="25" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/><TextBlock Text="总检测数" Foreground="{DynamicResource TextMutedBrush}"/></StackPanel></StackPanel></Border>
            <Border Grid.Column="1" Style="{StaticResource MetricCard}"><StackPanel Orientation="Horizontal"><TextBlock Text="✓" FontSize="28" Foreground="{DynamicResource SuccessBrush}" Margin="0,0,18,0"/><StackPanel><TextBlock Text="{Binding OkCount}" FontSize="25" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}"/><TextBlock Text="OK 数量" Foreground="{DynamicResource TextMutedBrush}"/></StackPanel></StackPanel></Border>
            <Border Grid.Column="2" Style="{StaticResource MetricCard}"><StackPanel Orientation="Horizontal"><TextBlock Text="⊗" FontSize="28" Foreground="{DynamicResource DangerBrush}" Margin="0,0,18,0"/><StackPanel><TextBlock Text="{Binding NgCount}" FontSize="25" FontWeight="Bold" Foreground="{DynamicResource DangerBrush}"/><TextBlock Text="NG 数量" Foreground="{DynamicResource TextMutedBrush}"/></StackPanel></StackPanel></Border>
            <Border Grid.Column="3" Style="{StaticResource MetricCard}" Margin="0"><StackPanel Orientation="Horizontal"><TextBlock Text="▥" FontSize="28" Foreground="{DynamicResource InfoBrush}" Margin="0,0,18,0"/><StackPanel><TextBlock Text="{Binding PassRateText}" FontSize="25" FontWeight="Bold" Foreground="{DynamicResource InfoBrush}"/><TextBlock Text="良率" Foreground="{DynamicResource TextMutedBrush}"/></StackPanel></StackPanel></Border>
        </Grid>
        <!-- 实时图像与配置 -->
        <Grid Grid.Row="3" Margin="0,0,0,12"><Grid.ColumnDefinitions><ColumnDefinition Width="1.05*"/><ColumnDefinition Width="1.3*"/></Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource SectionBorder}" Margin="0,0,10,0"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <Border Padding="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                    <DockPanel LastChildFill="True">
                        <Button DockPanel.Dock="Right" Content="{Binding PreviewPauseButtonText}" Command="{Binding PreviewPauseCommand}"
                                Padding="10,4" Margin="10,0,0,0" Background="{DynamicResource BrushBgSecondary}"
                                Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource BorderBrush}"/>
                        <TextBlock DockPanel.Dock="Right" Text="{Binding ImageDirectory}" Foreground="{DynamicResource TextMutedBrush}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal"><TextBlock Text="┃ 实时图像 · " Foreground="{DynamicResource InfoBrush}" FontWeight="Bold"/><TextBlock Text="{Binding CameraName}" Foreground="{DynamicResource TextPrimaryBrush}"/></StackPanel>
                    </DockPanel>
                </Border>
                <TextBlock Grid.Row="1" Text="{Binding FrameInfo}" Foreground="{DynamicResource TextMutedBrush}" Margin="12,8"/>
                <Grid Grid.Row="2" Background="#111827" Margin="12,0"><Image Source="{Binding PreviewImage}" Stretch="Uniform"/><Border Background="{Binding ResultColor}" CornerRadius="4" Padding="13,7" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12"><TextBlock Text="{Binding ResultText}" Foreground="White" FontWeight="Bold"/></Border></Grid>
                <TextBlock Grid.Row="3" Text="{Binding DetailText}" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="12,9"/>
            </Grid></Border>
            <Border Grid.Column="1" Style="{StaticResource SectionBorder}" Margin="10,0,0,0" Padding="16"><ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel>
                <TextBlock Text="┃ 相机与检测配置" Foreground="{DynamicResource InfoBrush}" FontWeight="Bold" Margin="0,0,0,8"/>
                <Grid Margin="0,0,0,12"><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition/></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition/><RowDefinition/></Grid.RowDefinitions><TextBlock Text="选择相机" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/><ComboBox Grid.Column="1" ItemsSource="{Binding Cameras}" SelectedItem="{Binding SelectedCamera}" DisplayMemberPath="DisplayText" Padding="7"/><TextBlock Grid.Row="1" Text="任务配方" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center" Margin="0,8,0,0"/><ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}" DisplayMemberPath="DisplayText" Padding="7" Margin="0,8,0,0"/></Grid>
                <TextBlock Text="相机源配置" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/><Grid Margin="0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="110"/><ColumnDefinition/></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions><TextBlock Text="驱动类型" Foreground="{DynamicResource TextMutedBrush}"/><TextBlock Grid.Column="1" Text="{Binding CameraDriver}" Foreground="{DynamicResource TextPrimaryBrush}"/><TextBlock Grid.Row="1" Text="图片目录" Foreground="{DynamicResource TextMutedBrush}"/><TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ImageDirectory}" Foreground="{DynamicResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/><TextBlock Grid.Row="2" Text="帧间隔" Foreground="{DynamicResource TextMutedBrush}"/><TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding FrameInterval, StringFormat={}{0} ms}" Foreground="{DynamicResource TextPrimaryBrush}"/><TextBlock Grid.Row="3" Text="循环播放" Foreground="{DynamicResource TextMutedBrush}"/><TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding LoopPlayback}" Foreground="{DynamicResource SuccessBrush}"/></Grid>
                <Separator Margin="0,8"/>
                <TextBlock Text="当前任务配方" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                <Border Visibility="{Binding HasNoRecipe, Converter={StaticResource BoolToVisibility}}" Background="#1FB45309" BorderBrush="#B45309" BorderThickness="1" CornerRadius="5" Padding="12" Margin="0,8"><TextBlock Text="当前相机尚未选择或创建配方任务，请点击“新增视觉任务”。相机实时预览不受影响。" Foreground="#F59E0B" TextWrapping="Wrap"/></Border>
                <ItemsControl ItemsSource="{Binding CurrentRecipe}" Margin="0,8"><ItemsControl.ItemTemplate><DataTemplate><Border Background="{DynamicResource BrushBgSecondary}" CornerRadius="4" Padding="9" Margin="0,0,0,5"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="34"/><ColumnDefinition Width="130"/><ColumnDefinition/></Grid.ColumnDefinitions><TextBlock Text="{Binding Order}" Foreground="{DynamicResource InfoBrush}" FontWeight="Bold"/><TextBlock Grid.Column="1" Text="{Binding Name}" Foreground="{DynamicResource TextPrimaryBrush}"/><TextBlock Grid.Column="2" Text="{Binding Parameters}" Foreground="{DynamicResource TextMutedBrush}" TextTrimming="CharacterEllipsis"/></Grid></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                <Separator Margin="0,10"/>
                <Border BorderBrush="{DynamicResource SuccessBrush}" BorderThickness="1" CornerRadius="5" Padding="12"><StackPanel><TextBlock Text="数据点注册 &amp; 报警" Foreground="{DynamicResource SuccessBrush}" FontWeight="Bold"/><Grid Margin="0,8,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="110"/><ColumnDefinition/></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions><TextBlock Text="ResourcePath" Foreground="{DynamicResource TextMutedBrush}"/><TextBlock Grid.Column="1" Text="{Binding ResultResourcePath}" Foreground="{DynamicResource SuccessBrush}"/><TextBlock Grid.Row="1" Text="NG 报警" Foreground="{DynamicResource TextMutedBrush}"/><TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding AlarmRuleName}" Foreground="{DynamicResource TextPrimaryBrush}"/><TextBlock Grid.Row="2" Text="NG 图片" Foreground="{DynamicResource TextMutedBrush}"/><TextBlock Grid.Row="2" Grid.Column="1" Text="自动按任务和日期保存" Foreground="{DynamicResource SuccessBrush}"/></Grid></StackPanel></Border>
            </StackPanel></ScrollViewer></Border>
        </Grid>
        <!-- 检测历史 -->
        <Border Grid.Row="4" Style="{StaticResource SectionBorder}"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><TextBlock Text="┃ 检测历史（当前运行）" Foreground="{DynamicResource InfoBrush}" FontWeight="Bold" Margin="14,10"/><DataGrid Grid.Row="1" ItemsSource="{Binding History}" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}" HeadersVisibility="Column"><DataGrid.Columns><DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat=HH:mm:ss.fff}" Width="120"/><DataGridTextColumn Header="图像编号" Binding="{Binding ImageName}" Width="2*"/><DataGridTextColumn Header="结果" Binding="{Binding ResultText}" Width="130"/><DataGridTextColumn Header="匹配度" Binding="{Binding Score, StringFormat=F3}" Width="100"/><DataGridTextColumn Header="耗时" Binding="{Binding Elapsed, StringFormat={}{0:F1} ms}" Width="100"/><DataGridTextColumn Header="报警" Binding="{Binding AlarmText}" Width="100"/><DataGridTextColumn Header="NG 图片" Binding="{Binding ImagePath}" Width="2*"/></DataGrid.Columns></DataGrid></Grid></Border>
    </Grid>
    <Border Margin="18" Visibility="{Binding IsTaskEditorOpen, Converter={StaticResource BoolToVisibility}}">
        <views:VisionTaskDialog DataContext="{Binding TaskEditor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Border>
    </Grid>
</UserControl>
